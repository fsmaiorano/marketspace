meta {
  name: Agent
  type: http
  seq: 3
}

post {
  url: {{ai_url}}/agent
  body: json
  auth: none
}

body:json {
  {
    "message": "What is the status of order {{order_id}}?",
    "userId": "{{customer_id}}"
  }
}
